apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cfgate-system

namePrefix: cfgate-

resources:
  - ../crd
  - ../rbac
  - ../manager

images:
  - name: controller
    newName: ghcr.io/cfgate/cfgate
    newTag: latest
